import loadData from '../utils/loadData'
import { PathFinder } from './PathFinder'

describe('Controller', () => {
    let controller: PathFinder
    beforeEach(() => {
        const data = loadData('src/day17/test.data')
    })

    afterEach(() => {})

    it('should be initialized', () => {})
})
